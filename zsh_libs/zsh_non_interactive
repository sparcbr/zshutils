VERSION=1.0

function debug()
{
	local cmd=$1
	shift
    set -x
    $cmd "$@"
    set +x
}

function runaswww()
{
    runas www-data  $@
}

function runas()
{
    user=$1
    shift
    sudo -u $user "$@"
}
